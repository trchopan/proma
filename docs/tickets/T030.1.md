---
id: T030.1
objective: 'Remove import project argument from CLI contract'
type: general
area: cli
surface: 'argument parsing and usage output'
priority: P1
status: done
branch: ''
base_branch: main
updated_at: '2026-03-13'
parent: T030
depends_on: []
spec_refs:
  - src/cli/args.ts
  - src/cli.ts
  - tests/cli.test.ts
  - README.md
---

# T030.1 - Remove import `--project` contract and usage references

## Problem / Goal

- The current import contract requires `--project`, which is no longer relevant when default output is stdout.

## Proposed Change

- Remove `--project` from import-only parsing rules and usage text.
- Keep mode validation unchanged (`--list-actions` xor `--tool`).

## Scope

In scope:

- Update import argument type and parser logic to drop `project`.
- Update CLI usage line for import command.

Out of scope:

- Output runtime/storage implementation details.

## Acceptance Criteria

- [x] `parseImportCommandArgs` does not parse or validate `--project`.
- [x] `ImportArgs` no longer includes `project`.
- [x] CLI usage text no longer shows `--project` for import.
- [x] Parser tests for import no longer include `--project`.

## Depends on

- T030
