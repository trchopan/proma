---
id: T001.4
objective: 'Wire digest CLI command and add comprehensive tests in tests/'
type: task
area: cli
surface: index.ts, tests/
priority: P1
status: pending
branch: ''
base_branch: main
updated_at: '2026-03-09'
parent: T001
depends_on:
  - T001.1
  - T001.2
  - T001.3
spec_refs: []
---

# T001.4 - CLI integration and test suite

## Problem / Goal

- The feature requires a usable CLI entrypoint and regression coverage in the `tests/` folder.

## Proposed Change

- Add `digest` command parsing in `index.ts`.
- Validate args: required `--input`, required `--project-name`, optional `--model`.
- Orchestrate read -> AI digest generation -> file write -> terminal result output.
- Add Bun tests under `tests/` for CLI errors/success and helper logic.

## Scope

In scope:

- Unit tests for output formatting and index allocation.
- Integration-style CLI test with mocked AI client.

Out of scope:

- End-to-end network tests against live OpenAI API.

## Acceptance Criteria

- [ ] `digest` command works for valid inputs and reports written paths.
- [ ] Invalid/missing args return readable errors.
- [ ] All new tests are located in `tests/` and pass with `bun test`.
