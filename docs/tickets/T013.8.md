---
id: T013.8
objective: 'Add tests for report args, resolvers, generation, and CLI integration'
type: test
area: testing
surface: 'tests/**/*.test.ts'
priority: P0
status: pending
branch: 'feature/T013-report-foundation'
base_branch: develop
updated_at: '2026-03-09'
parent: 'T013'
depends_on:
  - T013.7
spec_refs:
  - tests/cli.test.ts
  - tests/files.test.ts
---

# T013.8 - Add test coverage for report flow

## Problem / Goal

- Ensure report behavior is stable and safe to evolve.

## Proposed Change

- Add parser tests for report args and validation errors.
- Add resolver tests for `--input` and `--base` behavior.
- Add integration-style CLI tests for happy path and key failures.

## Scope

In scope:

- Unit and integration tests around the new report modules/paths.

Out of scope:

- Snapshot-heavy tests for stylistic prose variability.

## Implementation Notes

- Prefer deterministic fixtures and explicit date injection for file naming assertions.
- Keep test style aligned with existing bun test suites.

## Acceptance Criteria

- [ ] Report parser tests cover required args and invalid period.
- [ ] Resolver tests cover explicit and fallback modes.
- [ ] CLI tests verify success output and error handling paths.
- [ ] `bun test` passes with new tests.

## Depends on

- T013
- T013.7
