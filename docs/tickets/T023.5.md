---
id: T023.5
objective: 'Finalize merge-quality rollout with regression tests and documentation updates'
type: chore
area: quality
surface: 'test suite, fixture scenarios, README merge behavior docs'
priority: P1
status: pending
branch: ''
base_branch: develop
updated_at: '2026-03-11'
parent: 'T023'
depends_on:
  - T023.1
  - T023.2
  - T023.3
  - T023.4
spec_refs:
  - tests/digest.test.ts
  - tests/cli.test.ts
  - tests/files.test.ts
  - tests/topic-merge-service.test.ts
  - tests/prompting.test.ts
  - README.md
---

# T023.5 - Regression coverage and docs for merge-quality improvements

## Problem / Goal

- Merge-quality changes touch routing, merge semantics, and tag behavior, which can regress silently without focused tests and docs.
- We need end-to-end confidence and clear user-facing behavior documentation.

## Proposed Change

- Add/update regression tests across parser, prompt, service, and CLI layers for T023 behavior.
- Add a realistic fixture scenario derived from the release-cadence vs release-plan contamination case.
- Update README to document single-target routing, top-8 candidate ranking, semantic merge filtering, and tag reuse policy.

## Scope

In scope:

- Test updates in affected suites.
- Validation that dry-run/verbose paths remain coherent after T023 changes.
- Documentation updates in README merge behavior section.

Out of scope:

- New command-line flags for experimental merge modes.
- Performance benchmarking infrastructure.

## Implementation Notes

- Keep tests deterministic and independent from external API calls.
- Ensure test assertions focus on behavior contracts rather than prompt phrasing noise.

## Acceptance Criteria

- [ ] Regression tests cover all T023 acceptance criteria at appropriate layers.
- [ ] Representative contamination case is covered by test fixture/assertions.
- [ ] `bun test` passes locally after T023 changes.
- [ ] README clearly reflects updated merge behavior and constraints.
