---
id: T025.4
objective: 'Update tests and docs for category-specific topic structures and merge-content contracts'
type: feature
area: qa
surface: 'tests for merge/topic/report parsing, README topic section docs'
priority: P1
status: pending
branch: ''
base_branch: develop
updated_at: '2026-03-11'
parent: 'T025'
depends_on: ['T025.1', 'T025.2', 'T025.3']
spec_refs:
  - tests/topic-merge-service.test.ts
  - tests/files.test.ts
  - tests/digest.test.ts
  - tests/cli.test.ts
  - README.md
---

# T025.4 - Validation and documentation updates

## Problem / Goal

- Existing tests and docs encode the old shared topic section model.
- We need test coverage and docs that reflect category-specific structures and prevent regressions.

## Proposed Change

- Update unit/integration tests to assert category-specific section output and parser behavior.
- Add regression coverage for legacy topic read compatibility and new canonical writes.
- Update README topic-file behavior and generated section definitions per category.

## Scope

In scope:

- Test expectation updates for headings and merge-content payloads.
- New tests for category-specific parser/schema validation paths.
- README revisions for topic section contracts.

Out of scope:

- Additional tooling or docs outside repository README.

## Implementation Notes

- Keep tests deterministic and minimal; avoid over-coupling to non-functional formatting.
- Ensure each category has at least one dedicated assertion path in tests.

## Acceptance Criteria

- [ ] Test suite validates category-specific topic headings and field handling.
- [ ] Tests cover category-specific merge-content parser/schema behavior.
- [ ] Regression test(s) confirm legacy topic input parsing remains functional.
- [ ] README clearly documents required topic sections per category.
