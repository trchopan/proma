# Release v0.3.0

This release record is frozen. Amendments must be added as:
docs/releases/v0.3.0-amendment.md.

Branch: develop
Version: v0.3.0
Release Date: 2026-03-13

## Ticket Consolidation

### Architecture and code organization

#### Source reorganization into layered architecture
- Title: Source reorganization into layered architecture.
- Goal: Restructure modules into clear domain, storage, integration, core, and CLI layers without changing runtime behavior.
- Acceptance (condensed): Tree and imports are stable, behavior is unchanged, and CLI and tests continue to pass.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/files.ts`, `src/config.ts`, `src/errors.ts`, `src/logging.ts`, `src/digest/generate.ts`, `src/digest/parsers.ts`, `src/digest/render.ts`, `src/digest/schemas.ts`, `src/digest/types.ts`, `src/services/topic-merge.ts`, `src/report.ts`, `src/storage/digest-notes.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`, `src/storage/import-files.ts`, `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/import/github-client.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `tests/report.test.ts`, `tests/topic-merge-service.test.ts`.
- Dependencies: Migration sequencing across core and integrations, then domain, then storage, followed by verification cleanup.
- Disposition: Done.

### Import pipeline, CLI routing, and documentation

#### Generic MCP import workflow
- Title: Generic MCP import command, runtime, and persistence.
- Goal: Provide a generic import flow that can discover actions and execute tools against configured MCP servers.
- Acceptance (condensed): Server-targeted import actions execute correctly, output is written deterministically, and dry-run remains side-effect free.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/config.ts`, `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/storage/import-files.ts`, `src/files.ts`, `tests/cli.test.ts`, `tests/config.test.ts`, `tests/import.test.ts`, `README.md`.
- Dependencies: Stable import CLI contract, MCP config validation and deterministic server resolution.
- Disposition: Done.

#### GitHub import runtime and strict server namespace contract
- Title: Built-in GitHub import mode and namespaced import targeting.
- Goal: Add built-in GitHub import support through local `gh` while enforcing unambiguous target forms for built-in and MCP routes.
- Acceptance (condensed): `github` and namespaced MCP targets route correctly, bare names fail with migration guidance, and docs and tests cover success and failure paths.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/storage/import-files.ts`, `src/files.ts`, `tests/cli.test.ts`, `tests/import.test.ts`, `tests/config.test.ts`, `README.md`.
- Dependencies: Strict target parsing and import orchestration support for both built-in and MCP runtimes.
- Disposition: Done.

### Merge quality, contracts, and merge UX

#### Merge quality and routing improvements
- Title: Single-target routing, candidate ranking, semantic merge, and tag governance improvements.
- Goal: Increase merge relevance and consistency by narrowing routing to one target, improving candidate context, introducing semantic merge fallback safety, and reducing unnecessary tag proliferation.
- Acceptance (condensed): Routing picks one target, candidate narrowing is deterministic, semantic merge falls back safely on invalid output, and tag output remains stable.
- Canonical spec references: `src/cli.ts`, `src/digest.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `src/markdown/canonical-topic.ts`, `src/markdown/frontmatter.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/topic-merge-service.test.ts`, `tests/prompting.test.ts`, `README.md`.
- Dependencies: Routing contract update, candidate pre-ranking context, semantic merge integration, and reuse-first tag policy controls.
- Disposition: Done.

#### Category-specific canonical topic structures
- Title: Category-specific canonical structures across markdown, prompting contracts, and runtime wiring.
- Goal: Separate canonical topic content by category while keeping merge behavior and persistence deterministic.
- Acceptance (condensed): Category-specific fields parse and render consistently, runtime plumbing preserves behavior with fallback safety, and docs and tests are updated.
- Canonical spec references: `src/markdown/canonical-topic.ts`, `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`, `src/cli.ts`, `tests/topic-merge-service.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Category-aware markdown contract, category-aware merge-content contract, and runtime threading through merge and storage flows.
- Disposition: Done.

#### Merge lineage metadata and slug constraints
- Title: Merge lineage metadata correction and topic slug length guardrail.
- Goal: Correct lineage semantics and enforce bounded topic slug length while preserving idempotent behavior.
- Acceptance (condensed): Canonical lineage fields are written and read consistently, slug limits are enforced, and behavior remains deterministic.
- Canonical spec references: `src/cli.ts`, `src/digest.ts`, `src/prompting/registry.ts`, `src/services/topic-merge.ts`, `src/storage/digest-notes.ts`, `src/storage/topic-files.ts`, `src/markdown/frontmatter.ts`, `tests/cli.test.ts`, `tests/digest.test.ts`, `tests/files.test.ts`, `tests/topic-merge-service.test.ts`, `README.md`.
- Dependencies: Stable merge service behavior and canonical markdown/frontmatter handling.
- Disposition: Done.

#### Non-interactive merge control
- Title: Auto-merge CLI flag for non-interactive merge confirmation.
- Goal: Allow approved merge execution without prompts while preserving visibility and safety defaults.
- Acceptance (condensed): Auto-merge executes without confirmation prompts, default interactive flow remains intact, and dry-run behavior is unchanged.
- Canonical spec references: `src/cli/args.ts`, `src/cli.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Existing merge command and diff preview workflow.
- Disposition: Done.

### Runtime-configurable digest source policy

#### Config-driven digest source extension and alias removal
- Title: Runtime-configurable digest source policy.
- Goal: Allow project config to extend allowed digest sources with deterministic precedence while removing alias forms.
- Acceptance (condensed): Defaults are preserved without config, valid config extends source set, unknown and alias values are rejected, and downstream parsing and prompting respect the resolved policy.
- Canonical spec references: `src/config.ts`, `src/cli.ts`, `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/markdown/canonical-topic.ts`, `src/storage/digest-notes.ts`, `src/services/topic-merge.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/prompting.test.ts`, `README.md`.
- Dependencies: Config module loading, source resolver, and runtime propagation across ingest and merge paths.
- Disposition: Done.

### Prompting runtime simplification

#### Legacy plugin system removal
- Title: Removal of legacy plugin composition system.
- Goal: Simplify prompt execution by relying on built-in registry operations instead of plugin composition.
- Acceptance (condensed): Legacy plugin loading paths are removed, runtime remains stable under built-in operations, and docs reflect the simplified model.
- Canonical spec references: `src/cli.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/prompting/load.ts`, `src/prompting/plugin.ts`, `index.ts`, `tests/prompting.test.ts`, `README.md`, `PLUGIN_SYSTEM.md`.
- Dependencies: Stable built-in prompt registry and validation flow.
- Disposition: Done (partially superseded by later config evolution outside plugin scope).

### CLI safety and execution controls

#### Global dry-run guarantees
- Title: Global dry-run mode across CLI workflows.
- Goal: Ensure dry-run previews prevent all writes and external model execution while preserving useful operator feedback.
- Acceptance (condensed): Digest, merge, and report commands perform no writes in dry-run, model execution is blocked, and output clearly indicates planned actions.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/ai/openai.ts`, `src/prompting/execute.ts`, `src/logging.ts`, `tests/cli.test.ts`, `tests/openai.test.ts`.
- Dependencies: Consistent command orchestration and write-path centralization.
- Disposition: Done.

## Per-ticket extraction

### Architecture and code organization

#### Reorganize source tree into layered domains
- Title: Reorganize source tree into layered domains.
- Goal: Move modules into `core`, `integrations`, `domain`, `storage`, and `cli` folders without behavior drift.
- Acceptance (condensed): Imports and runtime behavior remain stable, tests continue passing.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/files.ts`, `src/digest/*`, `src/prompting/*`, `src/markdown/*`, `src/services/topic-merge.ts`, `src/storage/*`, `tests/cli.test.ts`.
- Dependencies: None.
- Disposition: Done.

#### Move core and integration modules first
- Title: Move core and integration modules first.
- Goal: Establish stable layer roots for shared runtime and provider modules.
- Acceptance (condensed): Modules are relocated with no runtime contract changes.
- Canonical spec references: `src/config.ts`, `src/errors.ts`, `src/logging.ts`, `src/ai/openai.ts`, `src/import/mcp-client.ts`, `src/import/github-client.ts`, `src/import/transform.ts`.
- Dependencies: None.
- Disposition: Done.

#### Move domain logic into domain layer
- Title: Move domain logic into domain layer.
- Goal: Co-locate digest, merge, and report domain behavior under a dedicated domain namespace.
- Acceptance (condensed): Relocated modules compile and preserve command behavior.
- Canonical spec references: `src/digest/generate.ts`, `src/digest/parsers.ts`, `src/digest/render.ts`, `src/digest/schemas.ts`, `src/digest/types.ts`, `src/services/topic-merge.ts`, `src/report.ts`.
- Dependencies: Core and integration relocation baseline.
- Disposition: Done.

#### Move storage modules and preserve facade compatibility
- Title: Move storage modules and preserve facade compatibility.
- Goal: Group storage by concern while keeping existing `files` facade behavior stable.
- Acceptance (condensed): Storage module paths are updated and callers keep working.
- Canonical spec references: `src/storage/digest-notes.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`, `src/storage/import-files.ts`, `src/files.ts`.
- Dependencies: Domain module relocation completed.
- Disposition: Done.

#### Verification and shim cleanup pass
- Title: Verification and shim cleanup pass.
- Goal: Validate full relocation and remove transitional compatibility shims.
- Acceptance (condensed): Test suites and CLI smoke checks pass with no stale legacy imports.
- Canonical spec references: `tests/cli.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `tests/report.test.ts`, `tests/topic-merge-service.test.ts`.
- Dependencies: Storage relocation and stable facade wiring.
- Disposition: Done.

### Import pipeline and command routing

#### Generic MCP import command
- Title: Generic MCP import command.
- Goal: Add an import command for MCP action discovery and tool execution.
- Acceptance (condensed): Server-targeted list and tool modes succeed with deterministic markdown output.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/config.ts`, `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/storage/import-files.ts`, `README.md`.
- Dependencies: None.
- Disposition: Done.

#### Import parser contract and orchestration
- Title: Import parser contract and orchestration.
- Goal: Define valid import mode combinations and route execution paths.
- Acceptance (condensed): Required flags and mutually exclusive modes validate clearly, dry-run stays side-effect free.
- Canonical spec references: `src/cli/args.ts`, `src/cli.ts`.
- Dependencies: None.
- Disposition: Done.

#### MCP config schema and deterministic resolution
- Title: MCP config schema and deterministic resolution.
- Goal: Support explicit MCP server config with strict validation.
- Acceptance (condensed): Valid server config loads, invalid config errors are actionable.
- Canonical spec references: `src/config.ts`, `tests/config.test.ts`.
- Dependencies: None.
- Disposition: Done.

#### MCP runtime and import persistence
- Title: MCP runtime and import persistence.
- Goal: Implement stdio JSON-RPC MCP execution and stable import-file persistence.
- Acceptance (condensed): Action discovery and tool invocation work, output path rules are deterministic.
- Canonical spec references: `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/storage/import-files.ts`, `src/files.ts`, `src/cli.ts`.
- Dependencies: Import parser contract and MCP config validation.
- Disposition: Done.

#### Import regression tests and docs
- Title: Import regression tests and docs.
- Goal: Lock import behavior with dedicated tests and user documentation.
- Acceptance (condensed): CLI, config, runtime, listing, and write paths are covered.
- Canonical spec references: `tests/cli.test.ts`, `tests/config.test.ts`, `tests/import.test.ts`, `README.md`.
- Dependencies: CLI, config, and runtime import implementation.
- Disposition: Done.

#### GitHub import runtime and namespaced targeting break
- Title: GitHub import runtime and namespaced targeting break.
- Goal: Add built-in GitHub imports and enforce explicit target namespaces.
- Acceptance (condensed): GitHub mode and MCP mode both work, ambiguous bare targets are rejected.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/import/mcp-client.ts`, `src/import/transform.ts`, `tests/cli.test.ts`, `tests/import.test.ts`, `README.md`.
- Dependencies: None.
- Disposition: Done.

#### Strict target parser for github and namespaced mcp
- Title: Strict target parser for github and namespaced mcp.
- Goal: Require `github` or `mcp.<name>` forms to avoid ambiguous routing.
- Acceptance (condensed): Valid forms parse and bare names fail with clear guidance.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`.
- Dependencies: None.
- Disposition: Done.

#### Built-in GitHub runtime via local gh
- Title: Built-in GitHub runtime via local gh.
- Goal: Provide GitHub action discovery and execution through the local `gh` binary.
- Acceptance (condensed): Action catalog and command execution work with explicit error handling.
- Canonical spec references: `src/import/mcp-client.ts`, `src/import/transform.ts`, `src/cli.ts`.
- Dependencies: Strict target parsing.
- Disposition: Done.

#### Unified orchestration for github and mcp imports
- Title: Unified orchestration for github and mcp imports.
- Goal: Route import execution by target type while preserving output and dry-run contracts.
- Acceptance (condensed): Both target classes execute through consistent orchestration paths.
- Canonical spec references: `src/cli.ts`, `src/files.ts`, `src/storage/import-files.ts`, `src/import/transform.ts`.
- Dependencies: Target parser and GitHub runtime support.
- Disposition: Done.

#### Coverage and migration docs for namespacing change
- Title: Coverage and migration docs for namespacing change.
- Goal: Document and test strict namespacing and built-in GitHub import behavior.
- Acceptance (condensed): Success and hard-failure paths are covered and migration notes are published.
- Canonical spec references: `tests/cli.test.ts`, `tests/import.test.ts`, `tests/config.test.ts`, `README.md`.
- Dependencies: Completed two-target import orchestration.
- Disposition: Done.

### Merge quality and canonical topic contracts

#### Merge quality initiative
- Title: Merge quality initiative.
- Goal: Improve merge target relevance, merged-content quality, and tag stability.
- Acceptance (condensed): Single-target routing, ranking, semantic merge fallback, and tag reuse controls are in effect with tests.
- Canonical spec references: `src/cli.ts`, `src/digest.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `tests/*merge*`, `README.md`.
- Dependencies: None.
- Disposition: Done.

#### Single-target routing contract
- Title: Single-target routing contract.
- Goal: Ensure each digest note resolves to at most one merge target.
- Acceptance (condensed): Parsing and execution enforce one target path per note.
- Canonical spec references: `src/digest.ts`, `src/cli.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`.
- Dependencies: None.
- Disposition: Done.

#### Candidate pre-ranking and richer routing context
- Title: Candidate pre-ranking and richer routing context.
- Goal: Narrow candidate set deterministically and improve prompt context quality.
- Acceptance (condensed): Candidate cap and context formatting are stable and predictable.
- Canonical spec references: `src/storage/topic-files.ts`, `src/services/topic-merge.ts`, `src/cli.ts`, `src/prompting/registry.ts`.
- Dependencies: Single-target routing baseline.
- Disposition: Done.

#### Semantic merge with deterministic fallback
- Title: Semantic merge with deterministic fallback.
- Goal: Use semantic content merge when valid and deterministic fallback on failure cases.
- Acceptance (condensed): Invalid, refusal, or runtime failures fall back safely without breaking idempotency.
- Canonical spec references: `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/services/topic-merge.ts`, `tests/topic-merge-service.test.ts`, `tests/prompting.test.ts`.
- Dependencies: Single-target routing behavior.
- Disposition: Done.

#### Reuse-first tag policy
- Title: Reuse-first tag policy.
- Goal: Favor existing tags and reduce unnecessary new-tag growth.
- Acceptance (condensed): Tag normalization and reuse logic remain stable across merges.
- Canonical spec references: `src/storage/topic-files.ts`, `src/services/topic-merge.ts`, `src/prompting/registry.ts`, `src/markdown/frontmatter.ts`, `tests/files.test.ts`.
- Dependencies: Ranked context and semantic merge integration.
- Disposition: Done.

#### Merge quality regression consolidation
- Title: Merge quality regression consolidation.
- Goal: Extend test and doc coverage for quality improvements.
- Acceptance (condensed): End-to-end merge quality behavior is captured in regression suites and docs.
- Canonical spec references: `tests/digest.test.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/topic-merge-service.test.ts`, `tests/prompting.test.ts`, `README.md`.
- Dependencies: Routing, ranking, semantic merge, and tag controls completed.
- Disposition: Done.

#### Category-specific topic structures end-to-end
- Title: Category-specific topic structures end-to-end.
- Goal: Split canonical structures and merge contracts by category.
- Acceptance (condensed): Category-specific sections and fields parse, merge, and persist consistently.
- Canonical spec references: `src/markdown/canonical-topic.ts`, `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`.
- Dependencies: None.
- Disposition: Done.

#### Category-aware markdown parser and renderer
- Title: Category-aware markdown parser and renderer.
- Goal: Implement deterministic category-specific canonical topic parsing and rendering.
- Acceptance (condensed): Category sections remain stable with compatibility for legacy heading reads.
- Canonical spec references: `src/markdown/canonical-topic.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`.
- Dependencies: None.
- Disposition: Done.

#### Category-specific merge-content prompt contract
- Title: Category-specific merge-content prompt contract.
- Goal: Make merge-content schemas and parsers category-discriminated and strict.
- Acceptance (condensed): Category outputs validate tightly without affecting unrelated operations.
- Canonical spec references: `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `tests/digest.test.ts`.
- Dependencies: Category-aware canonical markdown model.
- Disposition: Done.

#### Runtime plumbing for category-specific merge content
- Title: Runtime plumbing for category-specific merge content.
- Goal: Thread category-specific fields through merge CLI, services, storage, and reporting.
- Acceptance (condensed): Merge write and read paths carry category-specific data safely and idempotently.
- Canonical spec references: `src/cli.ts`, `src/services/topic-merge.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`, `tests/topic-merge-service.test.ts`, `tests/files.test.ts`, `tests/cli.test.ts`.
- Dependencies: Category-specific markdown and prompt contracts.
- Disposition: Done.

#### Category contract validation and docs update
- Title: Category contract validation and docs update.
- Goal: Confirm category-specific behavior across tests and public documentation.
- Acceptance (condensed): Validation, legacy compatibility, and docs coverage are complete.
- Canonical spec references: `tests/topic-merge-service.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Category-aware runtime wiring completed.
- Disposition: Done.

#### Merge lineage metadata correction and slug cap
- Title: Merge lineage metadata correction and slug cap.
- Goal: Correct lineage field semantics and enforce maximum topic slug length.
- Acceptance (condensed): Lineage metadata is canonicalized and slug bounds are consistently enforced.
- Canonical spec references: `src/cli.ts`, `src/digest.ts`, `src/prompting/registry.ts`, `src/services/topic-merge.ts`, `src/storage/digest-notes.ts`, `src/storage/topic-files.ts`, `src/markdown/frontmatter.ts`, `tests/cli.test.ts`, `tests/files.test.ts`.
- Dependencies: Stable frontmatter and merge persistence behavior.
- Disposition: Done.

#### Auto-merge execution flag
- Title: Auto-merge execution flag.
- Goal: Support non-interactive merge approvals for streamlined operator workflows.
- Acceptance (condensed): Auto-merge executes without prompts while preserving defaults and dry-run safety.
- Canonical spec references: `src/cli/args.ts`, `src/cli.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Existing merge orchestration path.
- Disposition: Done.

### Runtime-configurable digest source policy

#### Runtime-configurable source policy initiative
- Title: Runtime-configurable source policy initiative.
- Goal: Extend digest source policy from config with deterministic precedence and remove aliases.
- Acceptance (condensed): Configured and default sources are resolved deterministically and enforced end-to-end.
- Canonical spec references: `src/config.ts`, `src/cli.ts`, `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/markdown/canonical-topic.ts`, `src/storage/digest-notes.ts`, `src/services/topic-merge.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/prompting.test.ts`, `README.md`.
- Dependencies: Config loading and policy propagation across digest and merge flows.
- Disposition: Done.

#### Config module loading and source resolver
- Title: Config module loading and source resolver.
- Goal: Resolve effective source policy from project config with deterministic precedence.
- Acceptance (condensed): Valid configs load correctly, missing configs use defaults, invalid shapes fail with actionable errors.
- Canonical spec references: `src/config.ts`, `src/cli.ts`, `tests/cli.test.ts`.
- Dependencies: None.
- Disposition: Done.

#### Runtime source plumbing across validation and prompting
- Title: Runtime source plumbing across validation and prompting.
- Goal: Apply resolved source policy across schemas, parsers, prompts, markdown normalization, and merge paths.
- Acceptance (condensed): Unknown sources reject cleanly and alias behavior is fully removed.
- Canonical spec references: `src/digest/types.ts`, `src/digest/schemas.ts`, `src/digest/parsers.ts`, `src/digest/generate.ts`, `src/prompting/types.ts`, `src/prompting/registry.ts`, `src/markdown/canonical-topic.ts`, `src/storage/digest-notes.ts`, `src/services/topic-merge.ts`.
- Dependencies: Config resolver baseline.
- Disposition: Done.

#### Regression tests and docs for source policy
- Title: Regression tests and docs for source policy.
- Goal: Add confidence coverage for precedence, unions, rejection paths, and alias removal.
- Acceptance (condensed): Regression suites and documentation cover expected and failure scenarios.
- Canonical spec references: `tests/digest.test.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/prompting.test.ts`, `README.md`.
- Dependencies: Runtime source plumbing completed.
- Disposition: Done.

### Prompting runtime simplification

#### Legacy plugin system removal
- Title: Legacy plugin system removal.
- Goal: Remove plugin composition in favor of built-in prompt registry operations.
- Acceptance (condensed): Plugin runtime paths are retired and operator docs are aligned with built-in behavior.
- Canonical spec references: `src/cli.ts`, `src/prompting/registry.ts`, `src/prompting/validate.ts`, `src/prompting/load.ts`, `src/prompting/plugin.ts`, `index.ts`, `tests/prompting.test.ts`, `README.md`, `PLUGIN_SYSTEM.md`.
- Dependencies: Stable built-in prompt registry.
- Disposition: Done (partially superseded by later config evolution outside plugin scope).

### CLI safety and dry-run controls

#### Global dry-run mode
- Title: Global dry-run mode.
- Goal: Guarantee side-effect-free previews that skip writes and model execution.
- Acceptance (condensed): Digest, merge, and report dry-runs perform no writes, block model calls, and produce clear preview output.
- Canonical spec references: `src/cli.ts`, `src/cli/args.ts`, `src/ai/openai.ts`, `src/prompting/execute.ts`, `src/logging.ts`, `tests/cli.test.ts`, `tests/openai.test.ts`.
- Dependencies: Unified command orchestration and centralized write paths.
- Disposition: Done.
