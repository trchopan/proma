# Release v0.4.0

This release record is frozen. Amendments must be added as:
docs/releases/v0.4.0-amendment.md.

Branch: develop
Version: v0.4.0
Release Date: 2026-03-16

## Ticket Consolidation

### Import CLI and output behavior

#### Hard break import to stdout-first and project-free
- Title: Hard break import to stdout-first and project-free.
- Goal: Make import project-independent, default output to stdout, and only write files when `--output` is explicitly provided.
- Acceptance (condensed): Import no longer accepts `--project`, defaults to stdout, writes only to explicit output paths, and docs/tests reflect the hard break.
- Canonical spec references: `src/cli/args.ts`, `src/cli.ts`, `src/storage/import/import-files.ts`, `src/files.ts`, `tests/cli.test.ts`, `tests/import.test.ts`, `README.md`, `examples/intermediate.md`, `examples/advanced.md`.
- Dependencies: None.
- Disposition: Done.

#### Remove import --project contract and usage references
- Title: Remove import --project contract and usage references.
- Goal: Remove `--project` from import parsing and usage while keeping import mode validation unchanged.
- Acceptance (condensed): Import parser and type contracts no longer include project, and usage/tests are updated accordingly.
- Canonical spec references: `src/cli/args.ts`, `src/cli.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Foundational import contract transition to project-free execution.
- Disposition: Done.

#### Implement stdout-first import output flow
- Title: Implement stdout-first import output flow.
- Goal: Ensure import prints markdown to stdout by default and writes only to explicit output files.
- Acceptance (condensed): Runtime and dry-run behavior correctly reflect stdout-vs-file modes, with no default imports directory allocation.
- Canonical spec references: `src/cli.ts`, `src/storage/import/import-files.ts`, `src/files.ts`, `tests/cli.test.ts`, `tests/import.test.ts`.
- Dependencies: Updated import argument contract and parser behavior.
- Disposition: Done.

#### Align tests and docs with project-free import contract
- Title: Align tests and docs with project-free import contract.
- Goal: Align parser/runtime tests and public docs/examples with the stdout-first, explicit-output import model.
- Acceptance (condensed): Tests verify no-project parsing and stdout defaults; docs/examples remove project-coupled import behavior.
- Canonical spec references: `tests/cli.test.ts`, `tests/import.test.ts`, `README.md`, `examples/intermediate.md`, `examples/advanced.md`.
- Dependencies: Stable parser and runtime output behavior for import.
- Disposition: Done.

### Digest metadata and participant identity quality

#### Fix empty planning participants and enforce identity format
- Title: Fix empty planning participants and enforce identity format.
- Goal: Preserve and render ownership signals in planning topics using canonical identity formatting.
- Acceptance (condensed): Participant extraction and formatting are deterministic across sources, prompt guidance is updated, and regression coverage is added.
- Canonical spec references: `src/core/prompting/registry.ts`, `src/domain/digest/schemas.ts`, `src/domain/merge/topic-merge.ts`, `tests/digest.test.ts`, `tests/topic-merge-service.test.ts`, `README.md`.
- Dependencies: Reliable ownership signals in incoming digest content and deterministic merge normalization.
- Disposition: Done.

#### Harden participant normalization for malformed owner strings
- Title: Harden participant normalization for malformed owner strings.
- Goal: Normalize malformed owner text and deduplicate equivalent identities while preserving canonical participant output.
- Acceptance (condensed): Optional `@` forms are normalized, malformed display-name artifacts are suppressed, and duplicate identities collapse deterministically.
- Canonical spec references: `src/domain/merge/topic-merge.ts`, `tests/topic-merge-service.test.ts`, `tmp/media-portal/notes/planning_2026-03-15_4.md`, `tmp/media-portal/topics/planning/media-portal-workstream-status-and-coordination.md`.
- Dependencies: Existing participant extraction and canonical identity formatting pipeline.
- Disposition: Done.

#### Add input_raw to digest note metadata
- Title: Add input_raw to digest note metadata.
- Goal: Preserve raw-input provenance in digest note frontmatter with project-relative path preference.
- Acceptance (condensed): New notes include `input_raw`, merge-marking preserves it, path normalization is deterministic, and legacy notes remain readable.
- Canonical spec references: `src/cli.ts`, `src/storage/digest/digest-notes.ts`, `tests/files.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Stable digest write/parse lifecycle and frontmatter persistence behavior.
- Disposition: Done.

### Merge consolidation and topic creation guardrails

#### Improve topic consolidation with source-agnostic workstream routing
- Title: Improve topic consolidation with source-agnostic workstream routing.
- Goal: Reduce topic fragmentation through stronger consolidation signals while preserving hard split boundaries.
- Acceptance (condensed): Routing prefers durable workstreams, timebox and project anchors remain hard splits, and deterministic guardrails/tests/docs are in place.
- Canonical spec references: `src/core/prompting/registry.ts`, `src/storage/merge/topic-files.ts`, `src/cli.ts`, `src/domain/merge/topic-merge.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/topic-merge-service.test.ts`, `README.md`.
- Dependencies: Deterministic ranking and consolidation controls across merge orchestration.
- Disposition: Done.

#### Do not promote every merged PR into a decision topic
- Title: Do not promote every merged PR into a decision topic.
- Goal: Add deterministic promotion thresholds so routine git changes do not create low-value new decision topics.
- Acceptance (condensed): Low-signal git decision notes are blocked from creating new topics while meaningful or existing-topic merges continue as expected.
- Canonical spec references: `src/domain/merge/topic-merge.ts`, `src/storage/merge/topic-files.ts`, `tests/topic-merge-service.test.ts`, `tests/files.test.ts`, `README.md`.
- Dependencies: Stable decision routing path and deterministic merge guards.
- Disposition: Done.

#### Prevent near-duplicate planning topics in merge
- Title: Prevent near-duplicate planning topics in merge.
- Goal: Prevent creation of near-duplicate planning topics by consolidating or differentiating create-new outcomes deterministically.
- Acceptance (condensed): Duplicate-like create-new targets are suppressed or forced to use clear differentiators under hard-split boundaries, with test/docs alignment.
- Canonical spec references: `src/storage/merge/topic-files.ts`, `src/core/prompting/registry.ts`, `src/domain/digest/schemas.ts`, `src/domain/digest/parsers.ts`, `src/cli.ts`, `tests/files.test.ts`, `tests/cli.test.ts`, `tests/digest.test.ts`, `README.md`.
- Dependencies: Existing merge target consolidation pipeline and hard-split conflict rules.
- Disposition: Done.

#### Near-duplicate detection and consolidation fallback
- Title: Near-duplicate detection and consolidation fallback.
- Goal: Detect near-duplicate create-new planning identities and auto-consolidate to existing topics when hard splits do not require separation.
- Acceptance (condensed): Duplicate-like create-new routes convert to update-existing in safe cases, with hard-split safety preserved.
- Canonical spec references: `src/storage/merge/topic-files.ts`, `tests/files.test.ts`, `tests/cli.test.ts`.
- Dependencies: Deterministic identity normalization, similarity scoring, and existing hard-split checks.
- Disposition: Done.

#### Create_new identity differentiation under hard split
- Title: Create_new identity differentiation under hard split.
- Goal: Ensure required create-new topics remain clearly differentiated when hard split boundaries prevent consolidation.
- Acceptance (condensed): Create-new action is preserved under hard split and resulting identity stays deterministic and visibly distinct.
- Canonical spec references: `src/storage/merge/topic-files.ts`, `src/domain/merge/topic-merge.ts`, `tests/files.test.ts`, `tests/cli.test.ts`.
- Dependencies: Near-duplicate detection outcomes and hard-split boundary enforcement.
- Disposition: Done.

#### Prompt/docs/test alignment for duplicate guardrails
- Title: Prompt/docs/test alignment for duplicate guardrails.
- Goal: Align merge prompt guidance, docs, and regression coverage with deterministic near-duplicate routing rules.
- Acceptance (condensed): Prompt guidance discourages duplicate create-new naming, differentiator expectations are explicit, and strict structured validation remains intact.
- Canonical spec references: `src/core/prompting/registry.ts`, `src/domain/digest/schemas.ts`, `src/domain/digest/parsers.ts`, `tests/digest.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Stable deterministic duplicate guardrails and create-new differentiation behavior.
- Disposition: Done.

### Taxonomy migration to decision artifacts

#### Replace discussion with decision topic taxonomy
- Title: Replace discussion with decision topic taxonomy.
- Goal: Hard-switch canonical taxonomy from `discussion` to `decision` across digest, merge, and report flows.
- Acceptance (condensed): Category contracts, schemas, merge/report behavior, docs, and tests all enforce `planning|research|decision` with no discussion fallback.
- Canonical spec references: `src/domain/digest/types.ts`, `src/domain/digest/parsers.ts`, `src/domain/digest/schemas.ts`, `src/core/markdown/canonical-topic.ts`, `src/core/prompting/registry.ts`, `src/domain/merge/topic-merge.ts`, `src/cli.ts`, `src/storage/report/report-files.ts`, `tests/digest.test.ts`, `tests/files.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Coordinated category contract migration across schema, parser, prompting, merge, and report layers.
- Disposition: Done.

#### Replace digest category contract and structured-output validation with decision
- Title: Replace digest category contract and structured-output validation with decision.
- Goal: Update category enums and strict schema/parser validation to accept decision and reject discussion.
- Acceptance (condensed): Category unions and merge-content branches are updated, parser behavior is strict, and digest tests cover acceptance/rejection cases.
- Canonical spec references: `src/domain/digest/types.ts`, `src/domain/digest/parsers.ts`, `src/domain/digest/schemas.ts`, `src/core/prompting/registry.ts`, `tests/digest.test.ts`.
- Dependencies: Hard taxonomy migration baseline in digest and merge-content contracts.
- Disposition: Done.

#### Replace canonical discussion schema with decision schema in topic merge flow
- Title: Replace canonical discussion schema with decision schema in topic merge flow.
- Goal: Replace discussion-specific canonical topic parse/render and merge branching with decision-specific behavior.
- Acceptance (condensed): Discussion branches are removed, decision sections are rendered and merged end-to-end, and category-aware tests are updated.
- Canonical spec references: `src/core/markdown/canonical-topic.ts`, `src/domain/merge/topic-merge.ts`, `src/cli.ts`, `tests/files.test.ts`, `tests/topic-merge-service.test.ts`.
- Dependencies: Updated digest category contracts and strict decision-oriented merge-content handling.
- Disposition: Done.

#### Align report scanning, docs, and regression tests with hard taxonomy switch
- Title: Align report scanning, docs, and regression tests with hard taxonomy switch.
- Goal: Complete taxonomy migration in report inputs, user docs, and regression suites.
- Acceptance (condensed): Report validation and fallback scanning use planning/research/decision only, docs align, and full checks pass.
- Canonical spec references: `src/storage/report/report-files.ts`, `tests/cli.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `README.md`.
- Dependencies: Completed taxonomy updates in digest and merge layers.
- Disposition: Done.
