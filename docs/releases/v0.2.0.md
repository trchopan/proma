# Release v0.2.0

This release record is frozen. Amendments must be added as:
docs/releases/v0.2.0-amendment.md.

Branch: develop
Version: v0.2.0
Release Date: 2026-03-10

## Ticket Consolidation

### CLI and command orchestration

#### Report command and runtime integration
- Title: Report command foundation, argument contract evolution, CLI orchestration, unified runtime integration, and weekly default period behavior.
- Goal: Add a first-class report workflow to CLI operations and unify digest, merge, and report execution through a registry-driven runtime with stable UX defaults.
- Acceptance (condensed): Report command routing and execution are stable; explicit and fallback input and base flows are deterministic; lifecycle logging and exit semantics are consistent; period defaults to weekly while invalid values still fail clearly.
- Canonical spec references: `README.md`, `src/cli.ts`, `src/cli/args.ts`, `src/logging.ts`, `src/digest.ts`, `src/report.ts`, `tests/cli.test.ts`.
- Dependencies: Prompt registry, plugin and config loader, report input and base resolvers, report writer.
- Disposition: Done (early explicit-period-only contract superseded by weekly-default behavior).

### Storage, file resolution, and output layout

#### Topic and report file handling
- Title: Report input resolution, base report resolution, report writer conventions, and topic hierarchy standardization.
- Goal: Ensure deterministic file discovery and persistence for report generation and align topic storage under a category-rooted layout.
- Acceptance (condensed): Explicit file lists and fallback scans behave predictably; missing or invalid paths fail clearly; report filenames and front matter follow canonical conventions; output directories are created as needed; storage contracts remain deterministic.
- Canonical spec references: `src/files.ts`, `src/storage/topic-files.ts`, `src/storage/report-files.ts`, `src/markdown/frontmatter.ts`, `tests/files.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Report command orchestration and report generation pipeline.
- Disposition: Done.

### Markdown and data model normalization

#### Canonical context and ingest metadata
- Title: Report context loading and normalization and canonical merged-ingest metadata consolidation.
- Goal: Normalize topic and base markdown into stable report context and remove redundant ingest metadata in favor of one canonical field while preserving compatibility.
- Acceptance (condensed): Structured context loading is deterministic; errors identify problematic files; canonical metadata is written going forward; legacy metadata remains safely readable for dedupe and idempotency.
- Canonical spec references: `src/markdown/frontmatter.ts`, `src/markdown/canonical-topic.ts`, `src/services/topic-merge.ts`, `tests/topic-merge-service.test.ts`, `tests/files.test.ts`, `README.md`.
- Dependencies: File resolution and merge and topic services.
- Disposition: Done.

### AI prompting runtime and plugin control plane

#### Registry-driven prompting architecture
- Title: Prompt control-plane types, built-in registry, plugin and config loading, startup validation guardrails, and legacy runtime removal.
- Goal: Centralize operation contracts for digest, merge, and report and support deterministic, safe customization via plugins.
- Acceptance (condensed): Built-ins are registry-backed with strict schemas and parsers; plugin composition is deterministic; invalid registries fail fast with actionable errors; unified runtime powers all commands; obsolete markdown-template runtime is removed.
- Canonical spec references: `src/cli.ts`, `src/digest.ts`, `src/report.ts`, `src/config.ts`, `src/ai/openai.ts`, `src/errors.ts`, `src/prompt-template.ts`, `prompts/digest.md`, `prompts/merge.md`, `prompts/report.md`.
- Dependencies: Runtime contract typing, registry implementation, config and plugin loading.
- Disposition: Done (earlier prompt-template generation path superseded).

### Packaging and external consumption

#### Installable package and cross-repo plugin support
- Title: External-consumption packaging, CLI bin and module entrypoint split, JavaScript config discovery, and weekly-default report ergonomics.
- Goal: Make the project consumable from other repositories as both executable CLI and importable module.
- Acceptance (condensed): Install exposes a runnable binary; root import is side-effect safe; JavaScript config files are discovered and loaded deterministically; invalid plugin declarations fail clearly; docs and tests reflect consumer workflows.
- Canonical spec references: `package.json`, `index.ts`, `src/cli.ts`, `src/prompting/load.ts`, `src/prompting/plugin.ts`, `src/cli/args.ts`, `tests/prompting.test.ts`, `tests/cli.test.ts`, `README.md`.
- Dependencies: Plugin architecture and runtime unification.
- Disposition: Done.

### Testing and quality coverage

#### Coverage expansion for report and prompting systems
- Title: Comprehensive report-flow tests and control-plane and runtime coverage.
- Goal: Raise confidence in CLI behavior, file resolution, registry and plugin composition, validation failures, and runtime integration.
- Acceptance (condensed): Test suites verify argument validation, fallback and explicit resolution, success and failure command paths, registry and plugin composition, validation errors, and unified runtime behavior.
- Canonical spec references: `tests/cli.test.ts`, `tests/files.test.ts`, `tests/digest.test.ts`, `tests/report.test.ts`, `tests/prompt-template.test.ts`, `tests/prompting.test.ts`.
- Dependencies: Report command, unified runtime, plugin and registry guardrails.
- Disposition: Done.

### Documentation and developer guidance

#### Report and plugin documentation updates
- Title: Report usage docs, runtime and plugin migration guidance, external setup guide, dedicated plugin documentation with examples and troubleshooting, and in-code API docs.
- Goal: Improve adoption and maintainability by documenting operation, customization, migration, and failure diagnosis paths.
- Acceptance (condensed): README and dedicated plugin guide are consistent with runtime behavior; practical examples are runnable; troubleshooting maps common failure modes to fixes; API docs improve discoverability in editors.
- Canonical spec references: `README.md`, `PLUGIN_SYSTEM.md`, `src/prompting/plugin.ts`, `src/prompting/load.ts`, `src/prompting/registry.ts`, `src/prompting/types.ts`, `src/prompting/validate.ts`, `tests/prompting.test.ts`.
- Dependencies: Plugin support baseline and runtime architecture.
- Disposition: Done.
